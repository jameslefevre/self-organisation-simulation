turing interaction:

gloabal parameters:

  float suppressionDistDecay = log(2.0) / 100;
  int suppressionMaxRange = 300;
  int maxBinSuppresion;

  int binSize = 100;
  int[] binArrayDim;

  proportionChange = 0.5; 

setup:

  maxBinSuppresion = 1 + suppressionMaxRange / binSize;
  binArrayDim = ...

cell properties:

  float tipActivation; // marks "extreme tip", controller/driver of branching
  float repressionSignal; // negative feedback in turing mechanism (relatively long range, diffusible



pairwiseInteractions:

  ArrayList[][][] bc = binCells();
  
